# "오늘의 개념" 데일리 (텔레그램 푸시) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** How-to-CS의 기존 92개 노트 중 하루 하나를 복습엔진 기준으로 골라 옵시디언 노트 + 텔레그램 액티브 리콜 카드로 매일 전달한다.

**Architecture:** 단일 신규 스크립트 `_build/daily.py`가 (1) 노트 frontmatter 로드 → (2) 날짜 시드 복습우선 선정 → (3) `00_INDEX/🗓 오늘의 개념.md` 렌더 → (4) 텔레그램 발송(best-effort)을 담당한다. GitHub Actions cron이 매일 실행하고 변경분을 자동 커밋한다. 순수 함수(선정·포맷)와 IO(로드·발송·쓰기)를 분리해 테스트 가능하게 한다.

**Tech Stack:** Python 3 표준 라이브러리만 (`re`, `os`, `sys`, `datetime`, `hashlib`, `urllib`, `unittest`). 추가 pip 의존성 없음. GitHub Actions.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `_build/daily.py` (신규) | 선정+렌더+발송. 순수함수(`md_v2_escape`,`parse_fm`,`extract_*`,`body_first`,`pick_today`,`github_url`,`build_message`,`render_note`) + IO(`load_notes`,`send_telegram`,`main`) |
| `_build/test_daily.py` (신규) | `daily.py` 순수함수 unittest |
| `00_INDEX/🗓 오늘의 개념.md` (생성물) | daily.py가 매일 덮어씀. 사람이 직접 안 고침 |
| `_build/build_index.py` (수정) | Home 바로가기에 `[[🗓 오늘의 개념]]` 한 줄 추가 |
| `.github/workflows/daily.yml` (신규) | cron + workflow_dispatch → daily.py → 변경 시 commit/push |
| `README.md`, `CLAUDE.md` (수정) | 데일리 기능 문서화 |

**규칙 준수:** `_source/` 손대지 않음(daily.py는 커밋된 노트만 읽음). 카테고리 폴더 직접편집 없음. `🗓 오늘의 개념.md`는 생성물. stdout 이모지 회피를 위해 `main()` 진입 시 `sys.stdout.reconfigure(encoding="utf-8")`로 cp949 크래시를 차단(상태 로그는 이모지 없이 작성, dry-run 미리보기만 이모지 포함).

**테스트 실행:** `python _build/test_daily.py -v` (sys.path[0]=_build 이므로 `import daily` 가능).

---

### Task 1: daily.py 스캐폴드 + md_v2_escape (TDD)

**Files:**
- Create: `_build/daily.py`
- Create: `_build/test_daily.py`

- [ ] **Step 1: 실패 테스트 작성** — `_build/test_daily.py`

```python
# -*- coding: utf-8 -*-
import os, sys, unittest
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import daily


class TestEscape(unittest.TestCase):
    def test_escapes_markdownv2_specials(self):
        self.assertEqual(daily.md_v2_escape("a-b.c"), "a\\-b\\.c")
        self.assertEqual(daily.md_v2_escape("x_y!"), "x\\_y\\!")

    def test_leaves_korean_and_emoji_untouched(self):
        self.assertEqual(daily.md_v2_escape("뮤텍스 세마포어"), "뮤텍스 세마포어")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 실패 확인**

Run: `python _build/test_daily.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'daily'` (아직 파일 없음)

- [ ] **Step 3: 최소 구현** — `_build/daily.py`

```python
# -*- coding: utf-8 -*-
"""
How-to-CS : 오늘의 개념 데일리
- 기존 노트(8개 카테고리 + _inbox)에서 하루 하나를 복습우선+날짜시드로 선정
- 00_INDEX/🗓 오늘의 개념.md 렌더 + 텔레그램 발송(best-effort)
의존성: 표준 라이브러리만.
"""
import os, re, sys, hashlib, urllib.request, urllib.parse
from datetime import date

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
CATS = [
    ("01_운영체제", "운영체제"), ("02_네트워크", "네트워크"),
    ("03_데이터베이스", "데이터베이스"), ("04_자료구조", "자료구조"),
    ("05_알고리즘", "알고리즘"), ("06_디자인패턴", "디자인패턴"),
    ("07_컴퓨터구조", "컴퓨터구조"), ("08_소프트웨어공학", "소프트웨어공학"),
]
REPO_DEFAULT = "pyeree/How-to-CS"

_MD_V2 = re.compile(r"([_*\[\]()~`>#+\-=|{}.!\\])")


def md_v2_escape(s):
    """텔레그램 MarkdownV2 특수문자 이스케이프."""
    return _MD_V2.sub(r"\\\1", s or "")
```

- [ ] **Step 4: 통과 확인**

Run: `python _build/test_daily.py -v`
Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add "_build/daily.py" "_build/test_daily.py"
git commit -m "오늘의 개념: daily.py 스캐폴드 + MarkdownV2 이스케이프"
```

---

### Task 2: frontmatter/본문 추출 함수 (TDD)

**Files:**
- Modify: `_build/daily.py`
- Modify: `_build/test_daily.py`

- [ ] **Step 1: 실패 테스트 추가** — `_build/test_daily.py`의 `if __name__` 위에 클래스 추가

```python
NOTE_WITH_SUMMARY = """---
tags: [운영체제]
status: 완료
priority: 1
복습일: 2026-06-20
---
## 뮤텍스 vs 세마포어

본문 첫 문단입니다.

## 관련 개념
- [[Semaphore & Mutex]]
- [[Deadlock]]

<!-- 🔒 MANUAL:START — 빌드해도 안 지워짐. 30초 요약 등 직접 작성 -->
## 🎤 면접 30초 요약
> 본문을 30초 분량으로 압축.

뮤텍스는 1개, 세마포어는 N개를 동기화합니다.
<!-- 🔒 MANUAL:END -->
"""

NOTE_EMPTY_SUMMARY = """---
tags: [네트워크]
status: 안함
priority: 2
복습일:
---
## TCP 3-way handshake

핸드셰이크 본문 첫 문단.

## 관련 개념
- [[TCP/IP]]

<!-- 🔒 MANUAL:START — 빌드해도 안 지워짐. 30초 요약 등 직접 작성 -->
## 🎤 면접 30초 요약
> 본문을 30초 분량으로 압축.

<!-- 🔒 MANUAL:END -->
"""


class TestExtract(unittest.TestCase):
    def test_parse_fm(self):
        fm = daily.parse_fm(NOTE_WITH_SUMMARY)
        self.assertEqual(fm["status"], "완료")
        self.assertEqual(fm["priority"], "1")
        self.assertEqual(fm["복습일"], "2026-06-20")

    def test_parse_fm_empty_review(self):
        fm = daily.parse_fm(NOTE_EMPTY_SUMMARY)
        self.assertEqual(fm.get("복습일", ""), "")

    def test_extract_summary_present(self):
        s = daily.extract_summary(NOTE_WITH_SUMMARY)
        self.assertIn("뮤텍스는 1개", s)
        self.assertNotIn("본문을 30초", s)   # 안내 인용문 제거
        self.assertNotIn("면접 30초 요약", s)  # 헤딩 제거

    def test_extract_summary_empty(self):
        self.assertEqual(daily.extract_summary(NOTE_EMPTY_SUMMARY), "")

    def test_extract_related(self):
        r = daily.extract_related(NOTE_WITH_SUMMARY)
        self.assertIn("[[Semaphore & Mutex]]", r)
        self.assertIn("[[Deadlock]]", r)
        self.assertNotIn("MANUAL", r)

    def test_body_first(self):
        self.assertEqual(daily.body_first(NOTE_EMPTY_SUMMARY), "핸드셰이크 본문 첫 문단.")
```

- [ ] **Step 2: 실패 확인**

Run: `python _build/test_daily.py -v`
Expected: FAIL — `AttributeError: module 'daily' has no attribute 'parse_fm'`

- [ ] **Step 3: 구현** — `daily.py`의 `md_v2_escape` 아래에 추가

```python
def parse_fm(text):
    """frontmatter를 dict로. (build_index.py와 동일 방식, \\w가 한글 키도 매칭)"""
    fm = {}
    m = re.match(r"^---\n(.*?)\n---", text, re.S)
    if m:
        for line in m.group(1).splitlines():
            mm = re.match(r"(\w+):\s*(.*)", line)
            if mm:
                fm[mm.group(1)] = mm.group(2).strip()
    return fm


def extract_summary(text):
    """🔒 MANUAL 블록의 30초 요약 본문만. 헤딩/안내 인용문 제거. 없으면 ''."""
    m = re.search(r"MANUAL:START.*?-->(.*?)<!--\s*🔒\s*MANUAL:END", text, re.S)
    if not m:
        return ""
    block = m.group(1)
    block = re.sub(r"(?m)^#+ .*$", "", block)   # 헤딩 제거
    block = re.sub(r"(?m)^>.*$", "", block)      # 인용 안내문 제거
    return block.strip()


def extract_related(text):
    """'## 관련 개념' 섹션 본문(다음 헤딩/MANUAL 전까지). 없으면 ''."""
    m = re.search(r"(?m)^#+ 관련 개념\s*\n(.*?)(?=\n#+ |\n<!--|\Z)", text, re.S)
    return m.group(1).strip() if m else ""


def body_first(text):
    """frontmatter 제거 후 첫 '진짜' 문단(헤딩/인용/코드/주석/구분선 제외)."""
    body = re.sub(r"^---\n.*?\n---\n", "", text, flags=re.S)
    for para in re.split(r"\n\s*\n", body):
        p = para.strip()
        if not p or p[0] in "#>" or p.startswith(("<!--", "---", "```")):
            continue
        return p
    return ""
```

- [ ] **Step 4: 통과 확인**

Run: `python _build/test_daily.py -v`
Expected: PASS (전체 8 tests)

- [ ] **Step 5: 커밋**

```bash
git add "_build/daily.py" "_build/test_daily.py"
git commit -m "오늘의 개념: frontmatter/요약/관련개념/본문 추출 함수"
```

---

### Task 3: pick_today 선정 알고리즘 (TDD — 핵심)

**Files:**
- Modify: `_build/daily.py`
- Modify: `_build/test_daily.py`

- [ ] **Step 1: 실패 테스트 추가** — `_build/test_daily.py`

```python
from datetime import date


def _note(title, folder="01_운영체제", priority=2, status="안함", review=None):
    return {"title": title, "filename": title + ".md", "folder": folder,
            "category": "운영체제", "priority": priority, "status": status,
            "review_date": review, "summary": "", "related": "", "body_first": ""}


class TestPick(unittest.TestCase):
    TODAY = date(2026, 6, 22)

    def test_review_due_wins_and_earliest_first(self):
        notes = [
            _note("A", priority=1, status="안함"),
            _note("B", status="완료", review="2026-06-21"),
            _note("C", status="완료", review="2026-06-10"),  # 가장 오래된 복습
        ]
        picked, reason = daily.pick_today(notes, self.TODAY)
        self.assertEqual(picked["title"], "C")
        self.assertEqual(reason, "복습")

    def test_priority1_new_when_no_review(self):
        notes = [_note("A", priority=2, status="안함"),
                 _note("B", priority=1, status="안함")]
        picked, reason = daily.pick_today(notes, self.TODAY)
        self.assertEqual(picked["title"], "B")
        self.assertEqual(reason, "빈출신규")

    def test_any_new_when_no_priority1(self):
        notes = [_note("A", priority=2, status="안함"),
                 _note("B", priority=3, status="완료")]
        picked, reason = daily.pick_today(notes, self.TODAY)
        self.assertEqual(picked["title"], "A")
        self.assertEqual(reason, "신규")

    def test_completed_long_review_fallback(self):
        notes = [_note("A", status="완료"), _note("B", status="복습")]
        picked, reason = daily.pick_today(notes, self.TODAY)
        self.assertEqual(picked["title"], "A")
        self.assertEqual(reason, "장기복습")

    def test_none_when_empty(self):
        picked, reason = daily.pick_today([], self.TODAY)
        self.assertIsNone(picked)
        self.assertIsNone(reason)

    def test_deterministic_same_date(self):
        notes = [_note(c, priority=1, status="안함") for c in ["A", "B", "C", "D", "E"]]
        p1, _ = daily.pick_today(notes, self.TODAY)
        p2, _ = daily.pick_today(notes, self.TODAY)
        self.assertEqual(p1["title"], p2["title"])  # 같은 날 = 같은 결과

    def test_future_review_not_due(self):
        notes = [_note("A", priority=1, status="안함"),
                 _note("B", status="완료", review="2026-12-31")]  # 미래 = 미도래
        picked, reason = daily.pick_today(notes, self.TODAY)
        self.assertEqual(reason, "빈출신규")
        self.assertEqual(picked["title"], "A")
```

- [ ] **Step 2: 실패 확인**

Run: `python _build/test_daily.py -v`
Expected: FAIL — `AttributeError: module 'daily' has no attribute 'pick_today'`

- [ ] **Step 3: 구현** — `daily.py`에 추가

```python
def _seed(today):
    return int(hashlib.sha1(today.isoformat().encode("utf-8")).hexdigest(), 16)


def _pick_one(cands, today):
    """폴더+파일명 정렬로 결정성 확보 후 날짜시드로 택1."""
    cands = sorted(cands, key=lambda n: (n["folder"], n["filename"]))
    return cands[_seed(today) % len(cands)]


def pick_today(notes, today):
    """복습우선 선정. 반환 (note|None, reason|None)."""
    iso = today.isoformat()
    due = [n for n in notes if n["review_date"] and n["review_date"] <= iso]
    if due:
        earliest = min(n["review_date"] for n in due)
        tied = [n for n in due if n["review_date"] == earliest]
        return _pick_one(tied, today), "복습"
    g2 = [n for n in notes if n["priority"] == 1 and n["status"] == "안함"]
    if g2:
        return _pick_one(g2, today), "빈출신규"
    g3 = [n for n in notes if n["status"] == "안함"]
    if g3:
        return _pick_one(g3, today), "신규"
    g4 = [n for n in notes if n["status"] == "완료"]
    if g4:
        return _pick_one(g4, today), "장기복습"
    return None, None
```

- [ ] **Step 4: 통과 확인**

Run: `python _build/test_daily.py -v`
Expected: PASS (전체 15 tests)

- [ ] **Step 5: 커밋**

```bash
git add "_build/daily.py" "_build/test_daily.py"
git commit -m "오늘의 개념: 복습우선+날짜시드 선정 알고리즘 pick_today"
```

---

### Task 4: github_url + build_message (텔레그램 카드, TDD)

**Files:**
- Modify: `_build/daily.py`
- Modify: `_build/test_daily.py`

- [ ] **Step 1: 실패 테스트 추가** — `_build/test_daily.py`

```python
class TestMessage(unittest.TestCase):
    TODAY = date(2026, 6, 22)

    def test_github_url_encodes_path(self):
        u = daily.github_url("01_운영체제", "뮤텍스 vs 세마포어.md", "pyeree/How-to-CS")
        self.assertTrue(u.startswith("https://github.com/pyeree/How-to-CS/blob/main/"))
        self.assertNotIn(" ", u)  # 공백 인코딩됨

    def test_build_message_has_title_spoiler_link(self):
        note = _note("뮤텍스 vs 세마포어", priority=1, status="완료", review="2026-06-10")
        note["summary"] = "뮤텍스는 1개를 동기화."
        url = "https://github.com/pyeree/How-to-CS/blob/main/x"
        msg = daily.build_message(note, "복습", self.TODAY.isoformat(), url)
        self.assertIn("오늘의 개념", msg)
        self.assertIn("뮤텍스 vs 세마포어", msg.replace("\\", ""))  # 제목(이스케이프 무시)
        self.assertIn("||", msg)        # 스포일러로 요약 가림
        self.assertIn("⭐", msg)         # 빈출
        self.assertIn("🔁", msg)         # 복습
        self.assertIn(url, msg)         # 링크

    def test_build_message_empty_summary_hint(self):
        note = _note("TCP", priority=2, status="안함")
        note["summary"] = ""
        note["body_first"] = "핸드셰이크 설명"
        msg = daily.build_message(note, "신규", self.TODAY.isoformat(), "http://x")
        self.assertIn("핸드셰이크 설명", msg.replace("\\", ""))
        self.assertIn("채우기", msg)     # 작성 유도 문구
```

- [ ] **Step 2: 실패 확인**

Run: `python _build/test_daily.py -v`
Expected: FAIL — `AttributeError: module 'daily' has no attribute 'github_url'`

- [ ] **Step 3: 구현** — `daily.py`에 추가

```python
def github_url(folder, filename, repo, branch="main"):
    path = urllib.parse.quote(f"{folder}/{filename}")
    return f"https://github.com/{repo}/blob/{branch}/{path}"


def build_message(note, reason, today_iso, url):
    """텔레그램 MarkdownV2 액티브 리콜 카드."""
    e = md_v2_escape
    flags = ("⭐" if note["priority"] == 1 else "") + ("🔁" if reason == "복습" else "")
    summary = note["summary"] or note["body_first"]
    summary = summary[:600]
    lines = [
        f"🗓 오늘의 개념 · {e(note['category'])}  {flags}".rstrip(),
        "",
        f"*{e(note['title'])}*",
        "",
        e("30초 안에 설명해보세요  막히면 아래 펼치기"),
        "",
        f"||{e(summary)}||",
    ]
    if not note["summary"]:
        lines += ["", e("아직 30초 요약 없음 → 학습루프 1번 채우기 프롬프트로 작성")]
    lines += ["", f"[{e('노트 열기 (GitHub)')}]({url})"]
    return "\n".join(lines)
```

- [ ] **Step 4: 통과 확인**

Run: `python _build/test_daily.py -v`
Expected: PASS (전체 19 tests)

- [ ] **Step 5: 커밋**

```bash
git add "_build/daily.py" "_build/test_daily.py"
git commit -m "오늘의 개념: github_url + 텔레그램 카드 build_message"
```

---

### Task 5: render_note (옵시디언 생성물, TDD)

**Files:**
- Modify: `_build/daily.py`
- Modify: `_build/test_daily.py`

- [ ] **Step 1: 실패 테스트 추가** — `_build/test_daily.py`

```python
class TestRender(unittest.TestCase):
    def test_render_note_contains_key_parts(self):
        note = _note("뮤텍스 vs 세마포어", priority=1, status="완료", review="2026-06-10")
        note["summary"] = "뮤텍스는 1개."
        note["related"] = "- [[Deadlock]]"
        md = daily.render_note(note, "복습", "2026-06-22", "http://x")
        self.assertIn("# 🗓 오늘의 개념 (2026-06-22)", md)
        self.assertIn("[[뮤텍스 vs 세마포어]]", md)
        self.assertIn("뮤텍스는 1개.", md)
        self.assertIn("[[Deadlock]]", md)
        self.assertIn("[[🤖 Claude 학습 루프]]", md)
        self.assertIn("← [[🏠 Home]]", md)

    def test_render_note_empty_summary_hint(self):
        note = _note("TCP", status="안함")
        note["summary"] = ""
        note["body_first"] = "핸드셰이크 설명"
        md = daily.render_note(note, "신규", "2026-06-22", "http://x")
        self.assertIn("핸드셰이크 설명", md)
        self.assertIn("채우기", md)
```

- [ ] **Step 2: 실패 확인**

Run: `python _build/test_daily.py -v`
Expected: FAIL — `AttributeError: module 'daily' has no attribute 'render_note'`

- [ ] **Step 3: 구현** — `daily.py`에 추가

```python
def render_note(note, reason, today_iso, url):
    """00_INDEX/🗓 오늘의 개념.md 본문(옵시디언 마크다운)."""
    flags = ("⭐" if note["priority"] == 1 else "") + ("🔁" if reason == "복습" else "")
    if note["summary"]:
        summary = note["summary"]
    else:
        summary = (note["body_first"] +
                   "\n\n> ⓘ 아직 30초 요약 없음 — 노트에서 [[🤖 Claude 학습 루프]] ① 채우기")
    related = note["related"] or "- (관련 개념 없음)"
    return (
        f"# 🗓 오늘의 개념 ({today_iso})\n"
        f"> 매일 daily.py가 갱신하는 생성물. 직접 고치지 마세요.\n\n"
        f"## {note['title']}  · {note['category']} {flags}\n"
        f"[[{note['title']}]] 열기 · [GitHub]({url})\n\n"
        f"👉 먼저 30초로 떠올려보고, 아래 요약과 맞춰보세요.\n\n"
        f"### 🎤 30초 요약\n{summary}\n\n"
        f"### 🔗 관련 개념\n{related}\n\n"
        f"### 🤖 더 훈련하기\n"
        f"[[🤖 Claude 학습 루프]] ② 출제 / ③ 꼬리질문 프롬프트를 돌려보세요.\n\n"
        f"← [[🏠 Home]]\n"
    )
```

- [ ] **Step 4: 통과 확인**

Run: `python _build/test_daily.py -v`
Expected: PASS (전체 21 tests)

- [ ] **Step 5: 커밋**

```bash
git add "_build/daily.py" "_build/test_daily.py"
git commit -m "오늘의 개념: 옵시디언 노트 render_note"
```

---

### Task 6: load_notes + send_telegram + main 배선 (IO)

**Files:**
- Modify: `_build/daily.py`

- [ ] **Step 1: 구현** — `daily.py` 끝에 추가

```python
def load_notes(root):
    """8개 카테고리 + _inbox의 개념 노트(＋ 접두/비.md 제외)를 dict 리스트로."""
    notes = []
    folders = list(CATS) + [("_inbox", "내 노트")]
    for folder, category in folders:
        d = os.path.join(root, folder)
        if not os.path.isdir(d):
            continue
        for fn in sorted(os.listdir(d)):
            if not fn.endswith(".md") or fn.startswith("＋"):
                continue
            text = open(os.path.join(d, fn), encoding="utf-8").read()
            fm = parse_fm(text)
            notes.append({
                "title": fn[:-3],
                "filename": fn,
                "folder": folder,
                "category": category,
                "priority": int((fm.get("priority") or "2").strip() or "2"),
                "status": (fm.get("status") or "").strip(),
                "review_date": (fm.get("복습일") or "").strip() or None,
                "summary": extract_summary(text),
                "related": extract_related(text),
                "body_first": body_first(text),
            })
    return notes


def send_telegram(token, chat_id, text):
    """best-effort 발송. 성공 True / 실패 False(예외 삼킴)."""
    api = f"https://api.telegram.org/bot{token}/sendMessage"
    data = urllib.parse.urlencode({
        "chat_id": chat_id, "text": text,
        "parse_mode": "MarkdownV2", "disable_web_page_preview": "true",
    }).encode("utf-8")
    try:
        with urllib.request.urlopen(urllib.request.Request(api, data=data), timeout=15) as r:
            return r.status == 200
    except Exception as ex:
        print("telegram error:", ex)
        return False


def main(argv):
    sys.stdout.reconfigure(encoding="utf-8")  # cp949 콘솔에서 미리보기 이모지 크래시 차단
    dry = "--dry-run" in argv
    dpart = [a for a in argv if a.startswith("--date=")]
    today = date.fromisoformat(dpart[0].split("=", 1)[1]) if dpart else date.today()
    repo = os.environ.get("GITHUB_REPOSITORY", REPO_DEFAULT)

    notes = load_notes(ROOT)
    note, reason = pick_today(notes, today)
    if note is None:
        print("no candidate today")
        return

    url = github_url(note["folder"], note["filename"], repo)
    message = build_message(note, reason, today.isoformat(), url)
    note_md = render_note(note, reason, today.isoformat(), url)

    if dry:
        print("PICK:", note["folder"], "/", note["filename"], "/ reason:", reason)
        print("----- TELEGRAM PREVIEW -----")
        print(message)
        return

    out = os.path.join(ROOT, "00_INDEX", "🗓 오늘의 개념.md")
    with open(out, "w", encoding="utf-8") as f:
        f.write(note_md)
    print("wrote:", out)

    token, chat = os.environ.get("TELEGRAM_BOT_TOKEN"), os.environ.get("TELEGRAM_CHAT_ID")
    if token and chat:
        print("telegram sent" if send_telegram(token, chat, message) else "telegram failed (non-fatal)")
    else:
        print("telegram secrets missing - skip send")


if __name__ == "__main__":
    main(sys.argv[1:])
```

- [ ] **Step 2: 회귀 테스트 통과 확인**

Run: `python _build/test_daily.py -v`
Expected: PASS (21 tests 그대로 — IO 추가가 순수함수 안 깸)

- [ ] **Step 3: 실제 vault에서 dry-run**

Run: `python _build/daily.py --dry-run`
Expected: `PICK: ...` 한 줄 + `----- TELEGRAM PREVIEW -----` 아래 카드 출력, 에러 없음. (UnicodeEncodeError 안 나야 함)

- [ ] **Step 4: 고정 날짜 멱등 확인**

Run: `python _build/daily.py --dry-run --date=2026-06-22` 를 두 번
Expected: 두 번 모두 같은 PICK

- [ ] **Step 5: 커밋**

```bash
git add "_build/daily.py"
git commit -m "오늘의 개념: load_notes/send_telegram/main 배선 + --dry-run/--date"
```

---

### Task 7: build_index.py — Home에 오늘의 개념 링크

**Files:**
- Modify: `_build/build_index.py:69-72` (build_home의 바로가기 리스트)

- [ ] **Step 1: 수정** — `build_home()`의 `## 🚀 바로가기` 블록에 한 줄 추가

기존:
```python
             "## 🚀 바로가기",
             "- [[📊 진도 대시보드]] — 지금 뭐부터 볼지 / 진도율",
             "- [[🗺️ 학습 로드맵]] — Tier 순서 (빈출→심화)",
             "- [[🤖 Claude 학습 루프]] — Claude로 채우고 시험 보는 법", "",
```
변경 후:
```python
             "## 🚀 바로가기",
             "- [[🗓 오늘의 개념]] — 오늘 복습/공부할 단 하나 (매일 갱신)",
             "- [[📊 진도 대시보드]] — 지금 뭐부터 볼지 / 진도율",
             "- [[🗺️ 학습 로드맵]] — Tier 순서 (빈출→심화)",
             "- [[🤖 Claude 학습 루프]] — Claude로 채우고 시험 보는 법", "",
```

- [ ] **Step 2: 빌드 실행 + Home 링크 확인**

Run: `python _build/build.py`
Expected: `[OK]` 빌드 완료, 에러 없음.
Run: `grep -c "오늘의 개념" "00_INDEX/🏠 Home.md"`
Expected: `1`

- [ ] **Step 3: 데일리 노트 생성 후 링크 해소 확인**

Run: `python _build/daily.py --date=2026-06-22` (실제 노트 생성; 텔레그램 시크릿 없으면 skip)
Expected: `wrote: ...🗓 오늘의 개념.md`, `telegram secrets missing - skip send`
Run: `ls "00_INDEX/🗓 오늘의 개념.md"`
Expected: 파일 존재 (Home의 `[[🗓 오늘의 개념]]` 링크가 해소됨)

- [ ] **Step 4: 커밋**

```bash
git add "_build/build_index.py" "00_INDEX/🏠 Home.md" "00_INDEX/🗓 오늘의 개념.md"
git commit -m "오늘의 개념: Home 바로가기 링크 추가 + 첫 데일리 노트 생성"
```

---

### Task 8: GitHub Actions 워크플로우

**Files:**
- Create: `.github/workflows/daily.yml`

- [ ] **Step 1: 작성** — `.github/workflows/daily.yml`

```yaml
name: 오늘의 개념 데일리

on:
  schedule:
    - cron: "0 23 * * *"   # 08:00 KST (UTC+9)
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  daily:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: 오늘의 개념 선정 + 텔레그램 발송
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python _build/daily.py

      - name: 변경분 커밋
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "00_INDEX/🗓 오늘의 개념.md"
          git diff --staged --quiet || git commit -m "데일리: 오늘의 개념 갱신 ($(date +%F))"
          git push
```

- [ ] **Step 2: YAML 문법 검증**

Run: `python -c "import sys; print('ok')"` 후 (PyYAML 없으면) GitHub Actions의 빨간 줄 여부로 확인. 로컬 검증이 필요하면:
Run: `python -c "import json,re; t=open('.github/workflows/daily.yml',encoding='utf-8').read(); print('cron' in t and 'workflow_dispatch' in t)"`
Expected: `True`

- [ ] **Step 3: 커밋**

```bash
git add ".github/workflows/daily.yml"
git commit -m "오늘의 개념: GitHub Actions 데일리 cron 워크플로우"
```

---

### Task 9: 문서화 + 최종 검증

**Files:**
- Modify: `README.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: README.md에 섹션 추가** (파일 끝 적절한 위치)

```markdown
## 🗓 오늘의 개념 (데일리)

매일 08:00(KST) GitHub Actions가 `_build/daily.py`를 돌려 92개 노트 중 하나를
**복습우선 + 날짜시드**로 골라 `00_INDEX/🗓 오늘의 개념.md`로 갱신하고 텔레그램으로 푸시한다.

- 선정 순서: 복습 도래 → 빈출(priority 1) 미학습 → 미학습 → 완료(장기복습)
- 셋업: 텔레그램 `@BotFather` 봇 토큰 + chat_id 를 repo Secrets(`TELEGRAM_BOT_TOKEN`,
  `TELEGRAM_CHAT_ID`)에 등록. 미등록 시 노트만 갱신하고 발송은 건너뛴다.
- 수동 실행/미리보기: `python _build/daily.py --dry-run`
```

- [ ] **Step 2: CLAUDE.md 빌드 섹션에 한 줄 추가**

`## 빌드` 섹션 끝(의존성 줄 위)에 추가:
```markdown
- **데일리**: `python _build/daily.py`는 `build.py`와 독립(노트만 읽음, `_source` 불필요). `00_INDEX/🗓 오늘의 개념.md`는 생성물 — 직접 편집 금지, 고치려면 `_build/daily.py`를 고쳐라. 선정 로직/메시지 문구도 거기. `--dry-run`으로 미리보기. 텔레그램 시크릿은 GitHub Secrets에만.
```

- [ ] **Step 3: 전체 테스트 + dry-run 최종 확인**

Run: `python _build/test_daily.py -v`
Expected: PASS (21 tests)
Run: `python _build/build.py && python _build/daily.py --dry-run`
Expected: 빌드 `[OK]` + 데일리 미리보기 정상 출력, 에러 없음

- [ ] **Step 4: 커밋 + 푸시**

```bash
git add "README.md" "CLAUDE.md"
git commit -m "오늘의 개념: README/CLAUDE 문서화"
git push origin main
```

---

## 셋업 안내 (구현 후 사용자가 1회)

1. 텔레그램에서 `@BotFather` → `/newbot` → **봇 토큰** 확보
2. 그 봇과 대화 시작(아무 메시지) 후 브라우저에서
   `https://api.telegram.org/bot<토큰>/getUpdates` 열어 `chat.id` 확인
3. GitHub repo → Settings → Secrets and variables → Actions → New secret:
   `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`
4. Actions 탭에서 "오늘의 개념 데일리" → Run workflow 로 첫 발송 테스트
5. (옵션) 옵시디언 `obsidian-git` 자동 pull 설정
6. (옵션) cron 시각 조정 — `.github/workflows/daily.yml`의 `0 23 * * *`(=08:00 KST)
```
