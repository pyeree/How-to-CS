# 📊 진도 대시보드

> ⚠️ 이 페이지는 **Dataview 플러그인**이 있어야 자동집계됩니다. ([[🏠 Home]] 설정 참고)
> 공부 상태는 각 노트 상단 `status` 값을 바꾸면 자동 반영: **안함 → 공부중 → 완료 → 복습**

## 🎯 다음에 볼 것 (빈출·아직 안 함)
```dataview
TABLE WITHOUT ID file.link AS 개념, file.folder AS 분류
FROM "01_운영체제" OR "02_네트워크" OR "03_데이터베이스" OR "04_자료구조" OR "05_알고리즘" OR "06_디자인패턴" OR "07_컴퓨터구조" OR "08_소프트웨어공학"
WHERE status = "안함" AND priority = 1
SORT file.folder ASC
```

## 📖 지금 공부 중
```dataview
LIST FROM "01_운영체제" OR "02_네트워크" OR "03_데이터베이스" OR "04_자료구조" OR "05_알고리즘" OR "06_디자인패턴" OR "07_컴퓨터구조" OR "08_소프트웨어공학"
WHERE status = "공부중"
```

## 🔁 복습 대상
```dataview
LIST FROM "01_운영체제" OR "02_네트워크" OR "03_데이터베이스" OR "04_자료구조" OR "05_알고리즘" OR "06_디자인패턴" OR "07_컴퓨터구조" OR "08_소프트웨어공학"
WHERE status = "복습"
```

## 📈 분류별 진도율
```dataview
TABLE WITHOUT ID
  rows.file.folder[0] AS 분류,
  length(filter(rows, (r) => r.status = "완료")) AS 완료,
  length(rows) AS 전체,
  round(100 * length(filter(rows, (r) => r.status = "완료")) / length(rows)) + "%" AS 달성률
FROM "01_운영체제" OR "02_네트워크" OR "03_데이터베이스" OR "04_자료구조" OR "05_알고리즘" OR "06_디자인패턴" OR "07_컴퓨터구조" OR "08_소프트웨어공학"
GROUP BY file.folder
SORT 분류 ASC
```

## 🧮 전체 요약
```dataview
TABLE WITHOUT ID
  length(rows) AS 전체,
  length(filter(rows, (r) => r.status = "완료")) AS 완료,
  length(filter(rows, (r) => r.status = "공부중")) AS 공부중,
  length(filter(rows, (r) => r.status = "안함")) AS 안함
FROM "01_운영체제" OR "02_네트워크" OR "03_데이터베이스" OR "04_자료구조" OR "05_알고리즘" OR "06_디자인패턴" OR "07_컴퓨터구조" OR "08_소프트웨어공학"
GROUP BY true
```

← [[🏠 Home]]
